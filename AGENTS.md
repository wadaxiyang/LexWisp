# AGENTS.md

**适用项目：LexWisp**  
**适用范围：整个仓库，包括 GUI、业务模块、Windows 适配、存储、Provider 与 Updater**  
**维护日期：2026-09-16**

> 你的任务是实现一个轻量、可靠、容易继续开发的 Windows 应用，而不是展示框架技巧。**前端必须复用 GPUI-Kit，业务代码必须清晰可靠，性能必须用实际行为与测量证明。默认不新增脚本，默认复用现有测试入口，只为真实风险补充必要测试。**

## 1. 项目约束与工作原则

**固定技术栈为 Rust + GPUI + longbridge/gpui-kit。** 主程序采用单进程一体化架构；这里的“后端”指进程内的业务、网络、存储和平台能力，不是另建一个 HTTP 服务。只有更新安装使用按需运行的 `LexWisp.Updater.exe`。不得擅自引入 WebView、第二套 UI 框架、常驻后台进程、JavaScript 运行时或浏览器式前后端通信。

**版本与 API 以项目锁定源码为准。** GPUI-Kit 的门面、组件名称和 GPUI API 会随版本调整；不得把博客、旧 GPUI Component 示例、Zed 主分支代码与项目锁定依赖混搭。优先使用 `gpui_kit` 的统一入口及其匹配的组件导出，不再独立引入另一份不兼容的 `gpui`。确需直接依赖底层包时，确认来源、版本和类型一致，并检查依赖图。[R1][R2]

## 2. 必须优先复用 GPUI-Kit

### 2.1 组件选择与允许的自定义范围

**先找 Kit 现有组件、文档和 examples，再写业务视图。** 使用其已带样式的组件层作为默认方案；只有确实缺少所需行为时才使用底层能力进行小范围组合。不要绕过组件层，再用基础元素拼出另一套通用组件库。[R1]

| 界面需求 | 首选实现 | 不应新增的重复实现 |
|---|---|---|
| 按钮、提示、菜单与操作入口 | Kit 的 Button、Tooltip、菜单等组件及 Action 机制 | 自制按钮状态机、通用菜单和提示框系统 |
| 单行字段、多行查询输入 | 锁定版本的 Input、Textarea 及对应状态对象 | 自制文本编辑器、光标、选区、输入法处理 |
| 查询结果、选择与复制 | Kit 的 TextView、SelectableText 或锁定版本的等效能力 | 自己维护字形命中、选区绘制和复制算法 |
| Provider 选择、设置选项 | Kit 的选择器、Switch、Checkbox 等 | 仅靠 `div` 模拟完整交互控件 |
| 历史与收藏 | Kit 列表及虚拟化能力，确有多列需求时使用数据表格 | 自写通用虚拟列表、启动时构建所有历史行 |
| 对话框、通知、面板 | Kit 的 Dialog、Notification、Sheet 等覆盖层 | 第二套弹层、焦点陷阱和通知管理器 |
| 配色、字体、间距、图标 | Kit 主题、语义 tokens 与项目已有资源 | 散落的硬编码颜色和重复图标、字体资产 |

表中名称表示优先选型，具体导出路径与参数必须查锁定版本，不得臆造组件属性。一个功能不要引入能力明显过剩的控件，例如仅显示翻译结果却加载完整代码编辑器、语言服务或复杂停靠系统。

允许抽取 `ResultBody`、`ProviderForm`、`HistoryRow` 等**业务视图**，复用业务展示和交互组合。禁止为了统一外观再建立 `LexButton`、`LexInput`、`LexDialog` 等逐一转发 Kit API 的包装层；只有封装确实承担稳定的业务约束时才值得保留。业务视图不要依赖某个具体父窗口实例，应接收明确的数据和操作入口。

### 2.2 初始化、窗口根节点与交互

在应用初始化阶段调用 Kit 初始化逻辑；**每个真实窗口有自己的 `Root`，同一窗口内不为每个页面再建 Root**。按锁定版示例接入实际使用的对话框、通知和面板层，不假设只创建 Root 就完成全部覆盖层渲染。[R3]

Popup 是独立的操作系统窗口，不等于页面内 Popover。不要用组件弹层替代系统级 Popup，也不要为普通确认对话框多开一个原生窗口。窗口内的焦点、覆盖层、文本选择和快捷键作用域应遵循 Kit 的组织方式。[R3]

同一个动作的按钮、菜单和快捷键应调用同一个 Action 或业务命令。复用现有键盘导航、禁用态、焦点和选区行为；不要在根节点无条件拦截所有键鼠事件。只在明确需要时阻止事件传播，不在 `render` 中反复抢焦点。

## 3. 渲染与性能规则

### 3.1 保持渲染路径轻量

**`render` 只读取必要的展示状态并描述界面。** 禁止在其中发起网络请求、查询数据库、读取文件、阻塞等待、创建运行时、注册持久订阅、启动定时任务或执行大规模解析。点击回调也不能因为“不在 render 中”就执行这些阻塞工作。

每次渲染创建轻量元素描述是正常做法；需要跨帧存活的输入、焦点、滚动、选区与业务状态则由其所有者保存。值式展示优先采用普通构建函数或 `RenderOnce`，确有持续交互与状态时才使用实体视图。**`RenderOnce` 不是“这个组件永远只绘制一次”的缓存承诺。**[R4]

渲染中允许少量简单格式化与样式选择，不必把每个字符串都缓存。禁止复制整份历史记录、重复解析完整长文、重新构建所有行状态，或为了规避借用问题给整个大型状态结构加 `Clone` 并反复克隆。

只在实际改变了可见状态后通知更新，相关字段尽量合并修改。不要把每个流式片段都广播成全应用状态变化，也不要声称某次 `notify` 必然只重绘一个控件、完全不影响布局。更新范围是否足够小，应以实际渲染与布局成本判断。

### 3.2 稳定身份、列表与缓存

**需要保留交互状态的元素使用稳定 ID。** 列表优先使用记录 ID；排序、过滤后会改变的数组下标、每次生成的随机值、时间戳和翻译后的标签都不适合作为记录身份。同一父作用域内避免 ID 冲突。[R5]

历史与收藏采用数据库分页和 UI 虚拟化，二者不能相互替代。列表只持有必要的当前页、预取页和轻量索引，不在启动时加载全部正文。搜索与过滤在业务或存储层完成，行渲染器不得查库；批量记录需要关联数据时优先批量读取，避免逐行发起查询。

优先用 Kit 的既有滚动与虚拟化实现。明确谁拥有滚动，避免无意义的嵌套滚动容器；长文本和列表必须能够在布局中收缩。可变高度内容不能套用未经验证的固定行高算法。增量加载后尽量维持用户的选中项与滚动位置。

只缓存确实昂贵、能够定义失效条件的结果。缓存必须有容量或生命周期边界；文字布局、尺寸等缓存还要考虑文本、宽度、字体与 DPI 的变化。请求结束或窗口销毁后，不保留只对该次展示有用的大对象。不要建立一个无限增长的全局“通用缓存中心”。

### 3.3 文本与流式展示

结果缓冲区按实际输入增量追加，不为每个片段复制全部已有正文。解析协议与准备较重的展示数据离开 UI 热路径；不得把每次刷新都变成“从头解析全文”。对仅支持完整更新的 Kit API，可在有界正文上按批更新，先测量成本，不因此重写文字引擎。

复用 Kit 的文本输入、选区与复制能力，覆盖中文输入法、换行、混合语言和 emoji。不要用字节下标移动用户可见光标，也不要把一个 Unicode 标量值当作所有情况下的一个可见字符。用户选择文字或向上滚动时，不强制跳到末尾；只有处于跟随输出状态时才自动滚动。

**流式可视更新沿用 SPEC 的合并策略，默认最大约 30 Hz，而不是每个 token 刷一次。** 首个有效片段与最终状态应及时呈现；定时合并只在有待显示内容时运行。终止前按状态规则处理最后一批数据，不能因节流吞掉结尾文字、错误或完成状态。该频率是项目策略，不是框架性能测试结论。

## 4. 状态管理与所有权

### 4.1 单一事实来源，局部拥有状态

| 状态类别 | 推荐所有者 | 边界要求 |
|---|---|---|
| 查询生命周期、Provider 选择快照、请求标识 | 查询协调器或对应业务服务 | 不藏在按钮回调中，不依赖某个窗口是否存活 |
| 输入草稿、焦点、滚动、选区、菜单状态 | 当前窗口或业务视图及其 Kit 状态对象 | 不提升为全局状态；不跨窗口共享带窗口绑定的组件状态 |
| 已保存配置与当前设置草稿 | 配置服务与设置视图分别管理 | 草稿不等于生效配置；明确保存、应用与取消语义 |
| 历史、收藏及其持久化约束 | Repository 与数据库 | UI 只持有展示快照和选择状态，不直接执行 SQL |
| HTTP 客户端、运行时、平台服务 | 应用装配层管理的长期服务 | 不在每个页面各建一份，不变成无边界的万能 Service Locator |
| 临时任务、订阅、拖放状态 | 窗口、请求或对应操作的作用域 | 所有者消失时能够结束并释放资源 |

以明确的枚举表达互斥状态，例如空闲、取词中、请求中、流式接收、完成、失败、取消。不要用一组可以同时成立的 `is_loading`、`is_done`、`has_error` 拼出无法解释的组合。能从现有状态直接推导的标志，不要再保存成第二份可变事实。

UI 事件表达用户意图，业务服务决定规则和提交结果，UI 再展示确认后的状态。不要让 Popup、主窗口、托盘与快捷键分别维护一份“当前查询真相”。需要跨视图共享数据时共享领域数据或受控模型，不共享具有窗口专属焦点与布局状态的控件实例。

### 4.2 Entity、订阅与回调

`Entity` 克隆是句柄共享，不等于深拷贝内部数据；它也会延长实体生命周期。需要长期持有实体的所有者使用强引用，仅负责返回结果或监听的回调通常使用弱引用。避免 `Entity → Task/Subscription → Entity` 形成不必要的强引用保留链。弱引用升级失败视为接收方已离开，不视为需要重建窗口的理由。[R6]

不要把 GPUI 的可变上下文引用保存到结构体或带过异步等待。实体更新回到框架允许的上下文进行；避免在实体更新闭包里经另一条回调同步重入同一实体。需要顺序处理时使用现有命令入口或延后提交，不使用 `unsafe` 绕过借用规则。[R4][R6]

订阅应由明确对象保存并清理，不在每次渲染或每次数据刷新时重复注册。**不能把所有 `.detach()` 都理解为同一种行为。** 常见句柄的语义区别如下，具体以项目锁定实现为准。[R7][R8][R9][R10]

| 对象或机制 | 关键语义 | 本项目要求 |
|---|---|---|
| GPUI `Task` | 对已核验的上游版本，丢弃句柄会取消任务；`detach` 允许其继续 | 需要持续的任务保存句柄；不能随手 detach 窗口任务 |
| Tokio `JoinHandle` | 丢弃句柄会分离任务，**不会停止任务** | 显式取消，必要时 abort，并观察退出结果 |
| GPUI `Subscription` | 丢弃取消订阅；detach 后不再由该句柄控制取消 | 默认由合适的作用域持有，只有明确的长期订阅才考虑 detach |
| `WeakEntity` | 不拥有实体；升级可能失败 | 防止回调保活视图，但不能用来代替网络取消 |
| `CancellationToken` | 协作式取消信号，接收方必须参与处理 | 网络、桥接和子任务共同响应，不把信号误当作强制终止 |

## 5. 异步、并发与 Provider

### 5.1 执行位置必须明确

沿用工程 SPEC 已选方案：GPUI 管理 UI；网络使用**一个专用线程上的 Tokio current-thread runtime**；SQLite 使用单连接数据库线程；UIA 使用受控的专用线程。不得每次查询创建 runtime、线程或数据库连接，也不要另加第二套异步生态。

`async` 不会自动把阻塞工作移出 UI。GPUI 的异步上下文用于协调和回到 UI 更新，不代表其中任意代码都运行在后台。禁止在 UI 线程调用 `block_on`、阻塞 channel 接收、同步 HTTP、长时间文件操作或 `thread::sleep`。专用运行时线程驱动其事件循环不属于这条禁令。

CPU 密集型解析、压缩、哈希和同步阻塞操作进入既有后台执行能力，限制并发，不为每类操作再创建一套线程池。尤其不能把重计算放到 current-thread runtime 中饿死网络任务。锁、数据库事务和 GPUI 可变借用均不得跨不必要的 `.await`；先取得最小数据快照，释放访问，再等待外部工作。

为每个异步操作明确**所有者、截止条件、取消入口、结果接收方和收尾动作**。跨线程传递有界、拥有所有权的数据，不传裸引用和临时 Win32 缓冲区。命令队列满时明确拒绝、合并或背压，不在 UI 上阻塞等待空间。进度可合并，正文、错误、完成和事务结果不可静默丢弃。

### 5.2 最新请求生效，但取消与过期拦截必须分开

用户再次查询或刷新时，由协调器先推进请求序号，使旧结果失效，再取消旧任务并启动新任务。至少区分稳定的查询会话标识和单次请求序号，不能只用“输入文字相同”判断是不是同一请求。

```text
新意图 → 推进当前请求序号 → 取消旧请求 → 提交新任务
异步结果 → 核对会话与请求序号 → 核对接收方生命周期 → 应用状态变化
```

成功、失败、进度、部分文本和最终完成都必须通过同一个过期判定，不能只过滤成功结果。一个请求只能进入一次最终状态；已取消请求随后收到完成消息，不得再变回成功。窗口自己的延迟回调还要校验窗口代次，防止旧回调操作重新创建的窗口。

**“不显示旧结果”和“停止旧工作”必须同时实现。** 取消应传递到实际持有 HTTP 请求或响应流的任务，使其停止读取、释放响应，并结束关联桥接和合并任务；仅停止 UI 接收者仍可能留下网络工作。窗口级任务不得通过捕获强实体引用继续保活窗口。[R8][R9][R10]

每个请求设置有意义的连接、整体和流式停滞超时。刷新与快速重复热键也必须受并发上限约束，不能瞬间建立无限连接。取消后的自动重试必须停止，不在已经输出部分内容后无提示地重新请求，也不自动把用户文本转发给另一家 Provider。

Tokio 已经开始的 `spawn_blocking` 工作不能靠 abort 强制结束；外部 UIA/COM 调用也不能仅靠异步超时保证停止。超时后限制新的同类工作并进入受控降级，不不断另开替代线程，也不使用 `TerminateThread`。不能承诺本地取消一定停止远端推理或计费。[R9]

### 5.3 协议、缓冲和持久化边界

复用配置兼容的 `reqwest::Client` 及其连接池；克隆客户端句柄可用于共享，不为每个请求重建客户端，也不为了“线程安全”无理由套上外部大锁。具有不同代理或传输配置的客户端分别管理，不能强行共用一套不适用配置。[R11]

Provider adapter 只负责其协议差异，包括请求构造、鉴权方式、流式解码和错误映射。文本预处理、请求仲裁、超时、重试政策、历史保存和展示状态留在公共业务流程，不在各 Provider 复制。不要因为多个接口都叫“兼容接口”就假定其请求字段、结束标志和错误结构完全一致。

流式解析必须处理任意网络分块、多字节字符跨块、半条事件、正常终止和异常断开。为正文总量、单条事件和待消费缓冲设置上限；超限返回明确状态，而不是继续分配内存。使用背压时让取消能够打断等待，接收方关闭后生产者必须退出。

保存历史由业务层处理，不由 `render` 或每个流式回调处理。成功记录按请求 ID 去重；取消、失败、未完成内容按 SPEC 的既定策略处理。**显示用请求序号不能替代持久化授权校验。** 清空历史或关闭记录后，旧任务必须携带并校验已失效的 `privacy_epoch`，不能把旧内容重新写回；校验应在数据库提交边界生效。

用户已经确认的收藏、设置写入和已进入事务的操作，不随窗口消失而随意取消。UI 关闭不等于撤销已接受的业务命令。明确事务完成或回滚语义，不能用粗暴取消导致“界面说成功，数据库没保存”。

## 6. Popup、窗口生命周期与后台驻留

**Popup 懒创建，短时间复用；沿用 SPEC 默认隐藏 30 秒后销毁的策略。主窗口关闭即销毁。** 新任务到来时复用可用 Popup，取消旧的销毁定时器；定时回调执行前重新核验当前窗口代次、可见性与期限，避免刚显示又被旧计时器关闭。不要偷偷改成所有窗口永久隐藏。

窗口隐藏、请求取消、数据保留、窗口销毁是不同操作。按当前 Popup 固定、失焦与关闭策略执行，不写成一个含糊的 `close_everything()`。保留的查询结果应是业务数据，不是为了保存文字而保活整个 UI 树。隐藏后停止仅为该窗口服务的动画、持续布局和无意义刷新。

窗口真正销毁时释放其根视图、局部实体、焦点与滚动状态、订阅、定时器和临时缓存。检查注册表、闭包或服务是否还持有强引用。调用框架生命周期接口完成 GPUI 窗口销毁，不在框架不知情的情况下直接销毁其 HWND；原生补充操作集中在平台适配层。

应用驻留使用 SPEC 已确定的显式退出与原生托盘宿主方案，不靠永久隐藏一个完整 GPUI 主窗口。框架或驱动仍可能保留进程级缓存，不把“销毁窗口”宣传成“GPU 与内存一定全部归零”。后台空闲不得持续请求动画帧、忙等 channel，或定时扫描剪贴板和所有窗口。

取词时先捕获来源窗口与选区所需上下文，再执行可能改变焦点的 Popup 操作；读取完成前不得因弹窗激活而错误获取自己窗口的文字。复用 Windows 平台模块，不在各视图重复实现 UIA、模拟复制和剪贴板恢复。剪贴板回退须受明确许可及并发变化检测保护，不覆盖用户在操作期间新复制的内容。

定位考虑鼠标所在显示器、工作区边界、负坐标、任务栏和 DPI。明确 GPUI 逻辑坐标与 Win32 屏幕坐标的转换位置，不四处强制转换数值。失焦关闭策略要排除窗口自身菜单、对话框、文字选择与拖放交互；固定状态下不得误关。

退出时先拒绝新任务、注销入口并取消可取消工作，再完成必要写入、关闭数据库与平台资源，最后退出框架。更新安装必须等待主程序确认具备安全退出条件；不要为了减少代码或测试删掉签名校验、恢复日志、回滚和健康确认。

## 7. 拖放开发规则

### 7.1 先明确是哪一种拖放

**只有当前需求需要时才实现拖放。** 不因为 GPUI 支持拖放就增加文件导入、可拖拽仪表盘或复杂停靠界面。不同拖放类型有不同入口，不能混用。[R12][R13]

| 类型 | 实现边界 | 必须避免 |
|---|---|---|
| 应用内记录拖动、排序 | 优先 Kit 已有能力；缺口再用 GPUI 类型化拖放机制 | 自建全局鼠标钩子、每移动一个像素就写数据库 |
| 系统拖入文件或路径 | 核实锁定版本的 `ExternalPaths`、文件拖放事件及 Windows 实现 | 把路径事件误当成已获得文件内容或上传许可 |
| 外部应用拖入文字 | 先核实平台是否支持对应数据格式；有真实需求再做窄适配 | 假设内部 `on_drop` 自动接收任意外部文本、臆造 API |
| 移动 Popup 窗口 | 使用窗口移动能力和限定的拖动区域 | 用业务记录拖放模拟窗口移动，让整个结果区都变成拖动把手 |

`on_drag` 等接口位于对应 GPUI 交互 trait 中；类型、回调签名、接受条件与结束语义必须查锁定版实现。内部载荷携带记录 ID、来源和必要的小型快照，不携带整份数据库记录集合、密钥、窗口强引用或大型正文。[R12]

### 7.2 交互与提交

拖动开始时建立本次状态和轻量预览；移动期间只更新必要的悬停目标、插入位置或视觉提示，不不断创建预览实体和昂贵资源。只因位置变化就广播所有列表状态属于错误实现。需要边缘自动滚动时，限于当前拖动期间，拖动结束立即停掉任务。

放下前重新检查目标是否仍然存在、当前是否允许该操作，以及所用记录版本是否仍有效；预览阶段的合法性不能代替提交校验。成功放下只提交一次业务命令，排序等持久化修改放在一个事务中。目标非法、用户取消或提交失败时，恢复一致状态，不遗留半完成的乐观排序。

拖放会话结束、按 Esc、窗口关闭或失去必要的输入捕获时必须清理载荷、预览、高亮和自动滚动。仅离开某个目标时清理该目标悬停态，不错误终止仍可继续的整个拖动。外部文件的 Enter、Move、Leave 和 Drop 不可全部当成提交。

文字选择优先于窗口拖动，拖动把手不要覆盖结果文本、输入框和交互控件。键盘选择、文本复制、菜单与拖放应能共存。拖放不能是执行关键操作的唯一方式，至少保留对应的菜单或按钮入口。

### 7.3 Windows 与外部数据

GPUI 已处理的系统拖放不要再次注册。需要补充 OLE 时先检查窗口现有注册、线程与 COM apartment；不要在 UIA 的 MTA 线程直接套用窗口 OLE 方案。Win32 `RegisterDragDrop` 对既有注册、OLE 初始化及消息循环有明确要求，相关代码只能放进平台适配模块，资源按生命周期撤销。[R14]

外部数据均不可信。验证类型、大小和允许的操作；路径先当数据处理，不自动执行、递归读取目录或上传文件。实际文件读取放到后台并处理访问失败；扩展名不能代替内容校验。不要为了演示拖放而引入用户未要求的文件执行、自动导入或静默网络上传。

## 8. 业务代码必须和 GUI 一样扎实

### 8.1 复用规则与模块边界

**业务规则不能散落在渲染函数、点击回调和 Provider 中。** 查询、刷新、收藏、历史清理、配置应用各有一个明确的业务入口，各界面调用同一个入口。不要让同一校验规则在 GUI、数据库和 HTTP adapter 中分别演化成不同版本；必要的边界防御校验仍须保留。

沿用已有 workspace 的职责：核心类型不依赖 GPUI、SQLite 或 Win32；Provider 负责协议；Repository 负责持久化；平台层处理系统能力；应用层装配并协调；UI 负责展示和意图。不要为套用某个架构流派重新复制一套 `controller/service/manager/facade/use_case`。

优先直接函数、明确结构体和小范围组合。Trait 用在确有多实现或外部边界的位置，如 Provider、凭据和系统取词能力；不要给每个只用一次的 helper 配一个 Trait、泛型工厂和依赖注入容器。两个实现表面相似但语义不同，不应强行抽成布满条件分支的“万能方法”。

复用一个函数必须能解释它统一了哪条规则、哪段重复流程或哪种资源管理。不要为了消除几行简单重复，引入更难理解的宏、复杂泛型、动态分派链或跨模块循环依赖。业务模块按能力聚合相关逻辑，避免所有东西都堆进 `utils.rs`、`common.rs` 或一个巨大 `AppState`。

### 8.2 控制流与 Rust 实现质量

**先校验与提前返回，再走清楚的主流程。** 优先 `?`、`let ... else`、守卫和清晰的 `match`，减少连续 `if/else`、`match`、闭包的嵌套。出现三层以上的业务控制流时主动检查是否可拆分，但不要为了追求形式上的层数，把简单代码拆成十几个没有语义的函数。

函数围绕一个可命名的职责组织。长流程按“输入校验、准备、执行、提交、收尾”拆分，而不是按任意行数切割。简单循环优于难读的迭代器组合；不要为了显得 Rust 化堆叠 `map/and_then/transpose`。命名说明业务含义，注释解释约束、边界和取舍，不逐行复述语法。

优先借用、移动和小型不可变快照。共享句柄的克隆不必一概禁止，大型数据深拷贝则须有理由。不要把 `Arc<Mutex<Everything>>` 当作默认状态管理，也不要把本应单线程拥有的数据包装成线程安全对象来回传递。锁的作用域保持短小，不在锁内执行回调、网络或 UI 更新。

用户输入、文件、网络与系统调用错误通过 `Result` 处理，不使用 `unwrap`、`expect` 或 panic 作为正常控制流。确有内部不变量需要断言时说明依据；不要给所有错误兜底成空字符串、空列表或假成功。错误只在合适边界增加上下文，不层层包装同一信息。

`unsafe` 只允许出现在确有必要的底层适配中，必须写明有效性、所有权与线程约束并封装为小型安全接口。禁止用 `unsafe impl Send/Sync`、泄漏对象或静态可变状态解决普通生命周期问题。

### 8.3 存储、配置与错误呈现

数据库访问复用既有 Repository 和线程，不给每个页面建立连接与缓存副本。写入使用事务，查询采用参数绑定并考虑索引和分页。收藏等操作明确重复提交语义；UI 不应靠“按钮刚好禁用了”保证数据唯一性。

配置修改统一经过校验和配置服务。运行中任务使用开始时的有效配置快照；切换 Provider 或修改 API 配置不得让一次请求前后使用不同参数。API 密钥仅在必要时取得并限制生命周期，不进入普通配置、数据库导出、历史记录与诊断包。

区分用户取消、参数错误、无可用选区、鉴权失败、限流、网络超时、协议错误、数据库失败和内部错误。UI 给出简明可操作提示，内部保留脱敏上下文；正常取消不弹严重错误。日志不得包含原文、完整结果、剪贴板、API Key、鉴权头或未经处理的响应体，包含秘密的类型不能直接派生或打印完整 `Debug`。

诊断优先记录请求标识、状态、耗时、大小和资源计数。不要以“便于调试”为由默认启用逐 token 日志、全量请求抓取和高频性能采样。TLS 校验、凭据保护和更新验签不因实现精简而关闭。

## 9. 性能验证与长期稳定性

**先减少无用工作，再根据证据优化分配、布局或算法。** 不为了优化在简单路径提前引入对象池、无锁结构、自定义分配器或复杂缓存。不要使用强制压缩工作集之类手段制造“内存更低”的假象，也不以降低正确性、文本可选能力和取消可靠性换数字。

性能判断使用锁定依赖的 Release 构建；Debug、启用检查器与高频日志的结果不能冒充发布性能。构建沿用项目的 Release 配置，测试支持、story/gallery 和开发工具不应因随手开启所有 features 进入发布依赖。调整 LTO、代码生成单元等选项后再测，不把这些开关当作解决 UI 阻塞的替代品。

| 关注对象 | 至少区分的观察项 | 判定原则 |
|---|---|---|
| Popup 响应 | 冷创建、短时复用、首个反馈、首次有效内容 | 区分本地捕获/UI 延迟与 Provider 网络耗时 |
| 流式展示 | UI 更新时间、输入响应、正文规模和刷新次数 | 长结果期间仍能选择、复制、取消，不无界积压 |
| 后台空闲 | 工作集、私有提交量、CPU、线程及句柄数量 | 记录测量口径；隐藏到期后不持续渲染或明显周期唤醒 |
| 长期重复操作 | 窗口打开关闭、快速查询、取消、切换设置后的趋势 | 区分缓存预热后的平台与不断增长的资源泄漏 |
| 数据量增长 | 启动、分页、搜索和历史清理 | 不随历史条数把所有正文加载到 UI 内存 |

沿用 SPEC 的预算和既有测量方法，不在这里另造一个未经验证的“50 MB”或“稳定 120 FPS”承诺。只在相关热点修改时做聚焦的前后对比，记录硬件、构建方式、样本与口径；有足够样本时再给出分位数。无法运行 Windows 或完成测量就明确写“未验证”，不伪造结果。

保留少量有价值的资源计数或可按需启用的 tracing 点即可，不默认建设新的监控系统、基准框架或仪表盘。优化结论应能回答：哪段工作减少了、观察到了什么、是否损害其他交互，而不只是“代码更底层”。

## 10. 测试与脚本必须克制

### 10.1 总规则

**最重要的工程约束之一：不要加入大量无用的测试代码和脚本。** 默认新增脚本数为零；默认使用现有 Cargo、CI 和测试入口。没有新增真实风险时，可以不新增测试文件；有高风险行为时，不能以“保持精简”为由跳过必要验证。判断标准是能否发现实际故障，而不是测试数量、文件数量或覆盖率数字。

先扩展已有针对性测试；少量纯逻辑测试可放在模块内，真正跨模块行为才放集成测试。必要 Mock 仅模拟外部协议与边界，不 Mock 掉正在验证的业务逻辑。不要为测试再实现一份平行业务算法，然后用这份算法证明自身正确。

### 10.2 按变更风险选择最少的有效验证

| 变更 | 优先验证 | 不要增加 |
|---|---|---|
| 文案、颜色、间距和简单排版 | 编译与实际界面检查，复用已有交互检查 | 每个标签一个快照、只断言文本存在的测试套件 |
| 纯校验、协议解码、业务分支 | 少量正常、边界和失败用例，适合时使用表驱动 | getter/setter 测试、重复验证标准库行为 |
| 请求竞争、取消与旧结果隔离 | 可控事件顺序的针对性回归 | 任意 sleep、依赖付费 API 或真实公网的常规测试 |
| 历史、收藏、迁移和隐私清理 | 临时数据库上的真实事务和失败验证 | 全部 Mock 掉存储、只检查 SQL 字符串包含关键词 |
| 自己编写的 UI 交互组合 | 必要时使用 Kit 无头测试，驱动真实生产视图 | 重测 Kit 所有按钮、另建一套只用于测试的假界面 |
| Win32 取词、剪贴板、焦点、DPI 和系统拖放 | Windows 上聚焦的集成或人工验收 | 用 headless 测试冒充原生系统能力已验证 |
| 更新验签、替换和恢复 | 保留关键故障路径与可恢复性检查 | 为省测试而取消安全步骤，或仅断言程序能够启动 |

Kit 提供专门的测试能力；启用方式须与锁定版本一致，测试支持限制在开发/测试依赖图。UI 测试只补充普通单元测试不能覆盖的交互逻辑，无头测试不能证明真实 Windows 窗口、驱动、输入法或 OLE 行为正确。[R15]

异步测试优先控制任务就绪、取消和完成的顺序，使用现有能力与小型同步点，避免把时间碰运气作为断言依据。无需为此建设通用模拟调度器。不同请求先后完成、取消后晚到结果、窗口销毁后回调，以及清空历史后旧任务完成，是值得保留的少量关键回归。

### 10.3 明确禁止的测试和工具膨胀

不要生成以下内容：批量占位测试；仅扫描源码确认某个函数名存在的“架构测试”；对明显常量的测试；没有缺陷目标的海量快照；提交进仓库的大段调试输出；每个阶段一套 `verify_*`、`check_*`、`smoke_*` 包装脚本；作用相同的 PowerShell、Bash、Python 三套入口；只调用一条 Cargo 命令的脚本。

不要为一次性诊断建设新测试框架、下载大批依赖或加入常驻测试服务。临时探针、抓包、性能实验和生成文件默认不入库；若证明值得长期保留，优先压缩整合进现有测试或工具，而不是保留整个实验目录。

只有可重复的构建、打包、签名或发布流程确实需要自动化时，才扩展**现有** `xtask` 或脚本。复用一个入口，不为同一流程添加新包装层。不强制每个小改动都生成 ADR、阶段报告、测试报告和额外 README；必要结论写入已有文档或交付说明。

既有关键测试和发布工具不得因为本规则被批量删除。移除失效、重复或无效内容时确认其用途和替代验证，不借清理扩大本次修改范围。精简针对的是维护负担，不是正确性与安全保障。

## 11. Agent 的执行与完成标准

先定位当前功能的真实入口、状态所有者和可复用组件，选择最小充分改动。实现过程中同步检查取消、失败和释放路径；不要先交付一条看起来能演示的成功路径，再把所有边界写成 TODO。

优先直接使用仓库现有命令。以下是正常项目集成检查入口，不要求为它们生成脚本；局部开发先针对实际受影响的 package 或现有测试过滤器执行，交付前按影响范围补足验证。

```sh
cargo fmt --all -- --check
cargo check --workspace --locked
cargo clippy --workspace --all-targets --locked -- -D warnings
cargo test --workspace --locked
```

只有验证确实需要时才运行全量或 Release 检查；不要对每个文案修改反复构建所有示例、跑长期测试。不要默认加 `--all-features`，也不要修改 lockfile 或依赖范围来绕过原有构建约束。以上命令的实际可用性由仓库工作区和平台决定，执行失败时报告真实原因，不通过删除断言、禁用校验或伪造输出宣布成功。

完成前确认以下事项，并在交付说明中只报告本次实际相关的结论：

- **复用与范围**：使用了已有 Kit 组件和业务入口，没有引入第二套基础设施或无关功能。
- **状态与生命周期**：没有重复事实来源、过期结果污染、无主任务、残留订阅和关闭后保活窗口的问题。
- **性能与业务**：UI 不阻塞，正文与队列有界，关键事务、错误和隐私语义完整。
- **验证与仓库整洁**：必要检查真实执行；未执行项如实说明；没有多余测试、包装脚本、临时数据和未经请求的批量重构。

**交付标准是功能完整、逻辑直接、资源可控、验证充分且维护负担合理，而不是生成的代码、测试或脚本足够多。**

## 12. 参考资料与适用边界

本文件中的架构、默认值、限制与工作纪律属于 **LexWisp 的工程规定**，不是外部资料给出的通用性能结论。用户提供的 wsafight 文章是个人技术博客，仅用于理解概念，不作为 API 兼容性、生命周期或性能数字的最终依据。[R16]

特别避免沿用未经核验的旧版 `ViewContext`、`Model` 等用法；不能从“使用弱引用”推导“网络自动取消”，也不能混淆 GPUI 与 Tokio 的任务句柄语义。`impl IntoElement` 属于 `impl Trait` 用法，不应简单称为 `dyn Trait` 式 trait object。[R8][R9][R17]

下列官方页面用于核实相关概念。文档的 `latest` 会变化，其中独立 `gpui` API 页本次显示的是 **0.2.2**，并不等于 Kit 所用 GPUI 家族版本；它们不授权向项目额外引入该版本。实施时必须回到已锁定 Kit 版本及其依赖源码，核对方法和行为。本文未声称已在 Windows 上编译本项目或完成性能测试。

[R1]: https://github.com/longbridge/gpui-kit "GPUI Kit 官方仓库、README 与 examples"
[R2]: https://docs.rs/gpui-kit/latest/gpui_kit/index.html "GPUI Kit 官方门面与导出"
[R3]: https://gpui-kit.com/docs/getting-started/ "GPUI Kit 官方入门与窗口根节点"
[R4]: https://gpui-kit.com/docs/coding-guides/ "GPUI Kit 官方 Coding Guides"
[R5]: https://gpui-kit.com/docs/element_id/ "GPUI Kit 元素身份说明"
[R6]: https://zed.dev/blog/gpui-ownership "Zed 官方 GPUI ownership 说明"
[R7]: https://docs.rs/gpui/latest/gpui/struct.Subscription.html "GPUI Subscription API"
[R8]: https://docs.rs/gpui/latest/gpui/struct.Task.html "GPUI Task API"
[R9]: https://docs.rs/tokio/latest/tokio/task/struct.JoinHandle.html "Tokio JoinHandle、abort 与阻塞任务限制"
[R10]: https://docs.rs/tokio-util/latest/tokio_util/sync/struct.CancellationToken.html "Tokio-util 协作式取消"
[R11]: https://docs.rs/reqwest/latest/reqwest/struct.Client.html "reqwest 客户端与连接复用"
[R12]: https://docs.rs/gpui/latest/gpui/trait.StatefulInteractiveElement.html "GPUI 状态化交互与拖动开始接口"
[R13]: https://docs.rs/gpui-kit/latest/gpui_kit/struct.ExternalPaths.html "GPUI Kit 系统外部路径载荷"
[R14]: https://learn.microsoft.com/en-us/windows/win32/api/ole2/nf-ole2-registerdragdrop "Microsoft RegisterDragDrop 文档"
[R15]: https://gpui-kit.com/docs/test/ "GPUI Kit 官方测试指南"
[R16]: https://doc.rust-lang.org/reference/types/impl-trait.html "Rust Reference：impl Trait"
